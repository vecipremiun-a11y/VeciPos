# Fase 4 · Comparativa JSON vs Normalizado
*Generado: 2026-05-13T04:13:22.423Z*

> Solo informativo — la app aún NO lee desde sale_items / purchase_items.
> Esto muestra el potencial de speedup futuro.

**Empresa de muestra:** `default`

## Top-10 productos más vendidos último mes

### Plan — vía JSON (actual)
```sql
EXPLAIN QUERY PLAN
SELECT s.id, s.items FROM sales s
WHERE s.company_id = ?
AND s.date >= datetime('now', '-30 days')
AND s.status = 'completed'
```
- SEARCH s USING INDEX idx_sales_status (company_id=? AND status=? AND date>?)

### Plan — vía sale_items (normalizado)
```sql
EXPLAIN QUERY PLAN
SELECT product_id, SUM(quantity) AS qty
FROM sale_items
WHERE company_id = ?
AND sale_date >= datetime('now', '-30 days')
AND product_id IS NOT NULL
GROUP BY product_id
ORDER BY qty DESC LIMIT 10
```
- SEARCH sale_items USING INDEX idx_sale_items_company_date_product (company_id=? AND sale_date>?)
- USE TEMP B-TREE FOR GROUP BY
- USE TEMP B-TREE FOR ORDER BY

### Tiempo real (ejecución, no solo EXPLAIN)
- vía JSON:        1382 ms (11446 filas)
- vía normalizado: 226 ms (10 filas)
- speedup: **6.12×**

## Histórico ventas de un producto (90 días)

### Plan — vía JSON (actual)
```sql
EXPLAIN QUERY PLAN
SELECT id, items FROM sales
WHERE company_id = ?
AND date >= datetime('now', '-90 days')
```
- SEARCH sales USING INDEX idx_sales_company_date_id_desc (company_id=? AND date>?)

### Plan — vía sale_items (normalizado)
```sql
EXPLAIN QUERY PLAN
SELECT sale_date, quantity, price
FROM sale_items
WHERE company_id = ? AND product_id = ?
AND sale_date >= datetime('now', '-90 days')
ORDER BY sale_date DESC
```
- SEARCH sale_items USING INDEX idx_sale_items_company_product_date (company_id=? AND product_id=? AND sale_date>?)

### Tiempo real (ejecución, no solo EXPLAIN)
- vía JSON:        5868 ms (34469 filas)
- vía normalizado: 136 ms (0 filas)
- speedup: **43.15×**

## Ingresos por producto último mes

### Plan — vía JSON (actual)
```sql
EXPLAIN QUERY PLAN
SELECT s.items FROM sales s
WHERE s.company_id = ?
AND s.date >= datetime('now', '-30 days')
```
- SEARCH s USING INDEX idx_sales_company_date_id_desc (company_id=? AND date>?)

### Plan — vía sale_items (normalizado)
```sql
EXPLAIN QUERY PLAN
SELECT product_id, SUM(quantity * price * (1 - discount_pct/100.0)) AS revenue
FROM sale_items
WHERE company_id = ?
AND sale_date >= datetime('now', '-30 days')
AND product_id IS NOT NULL
GROUP BY product_id
ORDER BY revenue DESC
```
- SEARCH sale_items USING INDEX idx_sale_items_company_date_product (company_id=? AND sale_date>?)
- USE TEMP B-TREE FOR GROUP BY
- USE TEMP B-TREE FOR ORDER BY

### Tiempo real (ejecución, no solo EXPLAIN)
- vía JSON:        1104 ms (11468 filas)
- vía normalizado: 243 ms (1557 filas)
- speedup: **4.54×**
